<!-- TMPL_INCLUDE NAME="doc-head-open.inc" -->
<title>Koha &rsaquo; Administration système &Asaquo; amendes</title>
<!-- TMPL_INCLUDE NAME="doc-head-close.inc" -->
</head>
<body>
<!-- TMPL_INCLUDE NAME="header.inc" -->
<!-- TMPL_INCLUDE NAME="cat-search.inc" -->

<div id="breadcrumbs"><a href="/cgi-bin/koha/mainpage.pl">Accueil</a> &rsaquo; <a href="/cgi-bin/koha/admin/admin-home.pl">Administration</a> &rsaquo; Amendes</div>

<div id="doc" class="yui-t7">

<div id="bd">
    <div id="yui-main">
    <div class="yui-g">
    
    <h1>Définition <!-- TMPL_IF NAME="branch" -->règles de retard pour <!-- TMPL_VAR NAME="branch" --><!-- TMPL_ELSE -->règles de retard par défaut<!-- /TMPL_IF --></h1>
            
    <div class="help">
        <h3>CONSEIL pour les amendes</h3>
        <p>Chaque case doit être remplie avec l'amende, l'échéance et la durée des frais</p>
        <p>= 1 [Eur, USD] d'amende, au bout de 7 jours, par tranche de 7 jours</p>
        <p>Demandez à votre administrateur d'ajouter ce script dans le planificateur de tâches (crontab), après minuit, pour que les amendes soient calculées chaque nuit</p>
        <p>Les règles sont appliquées de la plus spécifique à la plus générale. La première qui est saisie :</p>
        <ul>
            <li>Même site, même catégorie</li>
            <li>Même site, catégorie par défaut</li>
            <li>Site par défaut, même catégorie</li>
            <li>Site par défaut, catégorie par défaut</li>
        </ul>
    </div>
        <form method="post" action="/cgi-bin/koha/admin/finesrules.pl">
        <label for="branch">Sélection d'un site : </label>
            <select id="branch" name="branch">
                <option value="">Par défaut</option>
                <!-- TMPL_LOOP NAME="branchloop" -->
                    <!-- TMPL_IF NAME="selected" -->
                        <option value="<!-- TMPL_VAR NAME="value" -->" selected="selected"><!-- TMPL_VAR NAME="branchname" --></option>
                    <!-- TMPL_ELSE -->
                        <option value="<!-- TMPL_VAR NAME="value" -->"><!-- TMPL_VAR NAME="branchname" --></option>
                    <!-- /TMPL_IF -->
                <!-- /TMPL_LOOP -->
            </select>
            <input type="submit" value="Sélectionner">
        </form>
        <form method="post" action="/cgi-bin/koha/admin/finesrules.pl">
            <input type="hidden" name="op" value="save" />
            <input type="hidden" name="branch" value="<!-- TMPL_VAR NAME="branch" -->" />
            
            <table>
            <caption>Définition des règles de retard pour <!-- TMPL_VAR NAME="branch" --></caption>
            <tr>
                <th>&nbsp;</th><!-- TMPL_LOOP NAME="title" --><th scope="col"><!-- TMPL_IF EXPR="in_title eq '*'" -->Par défaut<!-- TMPL_ELSE --><!-- TMPL_VAR NAME="in_title" --><!-- /TMPL_IF --></th><!-- /TMPL_LOOP -->
            </tr>
            <!-- TMPL_LOOP NAME="row" -->
                <tr>
                    <th scope="row"><!-- TMPL_IF name="total" -->Total <!-- TMPL_ELSE --><!-- TMPL_VAR NAME="categorycode" --><!-- /TMPL_IF --></th>
                    <!-- TMPL_LOOP NAME="cell" -->
                        <!-- TMPL_IF NAME="toggle" -->
                            <td class="highlight">
                        <!-- TMPL_ELSE -->
                            <td>
                        <!-- /TMPL_IF -->
                            <input name="<!-- TMPL_VAR NAME="finesname" -->" value="<!-- TMPL_VAR NAME="finesvalue" -->" size="6" maxlength="10" />
                        </td>
                    <!-- /TMPL_LOOP -->
                </tr>
            <!-- /TMPL_LOOP -->
        </table>
        <input type="submit" value="Valider les modifications">
    </form>

</div>
</div>
</div>
<!-- TMPL_INCLUDE NAME="intranet-bottom.inc" -->
